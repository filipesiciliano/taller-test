# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## Repository layout

This directory contains two independent projects (not a workspace — no shared root package.json):

- **`taller-test/taller-test/`** — a bare Vite + React 19 scaffold with an empty `App.tsx`. Essentially unused; ignore unless specifically asked.
